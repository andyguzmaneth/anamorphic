## Codebase Patterns
- Use `~/.cargo/bin/circom` (v2.2.3) for circuit compilation — the npm `circom` (v0.5.46) is the deprecated JS version
- Circom build output goes to `circuits/build/` (must create dir before compiling)
- Foundry project lives in `contracts/` with forge-std as a git submodule
- Rust workspace root is `relayer/Cargo.toml` with members: stealth-crypto (lib), relayer (bin)
- Root `package.json` has snarkjs and circomlib as devDependencies
- stealth-crypto uses k256 (secp256k1 ECDH/arithmetic), tiny-keccak (keccak256), rand, hex, aes-gcm, hkdf, sha2
- Instruction module: `Instruction` struct serializes to 141 bytes fixed-width big-endian; `encrypt_instruction` / `decrypt_instruction` use HKDF(shared_secret) -> AES-256-GCM
- Encryption format: nonce (12 bytes) || ciphertext || GCM tag — aes-gcm crate appends tag to ciphertext internally
- Stealth address protocol: ECDH shared secret -> keccak256(compressed point) -> reduce to scalar -> scalar*G + spend_pubkey
- `keccak256` and `pubkey_to_eth_address` are public in stealth_address module for reuse
- Solc 0.8.21 has an internal Natspec compiler bug — pin `solc_version = "0.8.24"` in foundry.toml
- StealthEscrow uses 7-day challenge period and 14-day recovery timeout (total 21 days before user recovery)
- MockERC20 and MockAMM are in `contracts/src/` — use for testing swaps in later stories (US-009, US-010, US-011)
- MockAMM constant-product formula: `amountOut = (amountIn * reserveOut) / (reserveIn + amountIn)` (no fees)
- DeployMockAMM script creates TokenA, TokenB, MockAMM with 100k initial liquidity each
- Circomlib Poseidon template: `Poseidon(nInputs)` supports up to 16 inputs; with 8 inputs t=9, produces 1171 constraints
- circomlibjs npm package provides JS Poseidon implementation for computing expected hashes in test scripts
- Circuit public inputs declared via `component main {public [field_name]} = Template()` syntax
- `npx snarkjs wtns check <r1cs> <witness>` verifies witness satisfies all R1CS constraints
- `scripts/compile.sh` compiles instruction_commitment circuit; `scripts/test_witness.sh` runs full witness test

---

## 2026-02-13 - US-001
- What was implemented: Project scaffold with Foundry, Rust cargo workspace, and Circom directories
- Files changed:
  - `.gitignore` — project-wide ignores
  - `contracts/` — Foundry project (forge init) with forge-std submodule
  - `relayer/Cargo.toml` — workspace config
  - `relayer/stealth-crypto/` — lib crate
  - `relayer/relayer/` — bin crate
  - `circuits/test_circuit.circom` — trivial Multiplier circuit
  - `package.json` — snarkjs + circomlib devDeps
- **Learnings for future iterations:**
  - `forge init` creates its own git repo — need to remove `.git` dir and re-add forge-std as submodule of parent
  - Circom Rust compiler must be built from source (`git clone https://github.com/iden3/circom.git && cargo build --release`)
  - The npm circom package is deprecated v0.5.46; always use `~/.cargo/bin/circom` (v2.2.3)
  - Circom requires output directory to exist before compilation (`mkdir -p circuits/build/`)
---

## 2026-02-13 - US-002
- What was implemented: Stealth address library in Rust using ECDH on secp256k1
- Files changed:
  - `relayer/stealth-crypto/Cargo.toml` — added k256, tiny-keccak, rand, hex dependencies
  - `relayer/stealth-crypto/src/lib.rs` — module re-export
  - `relayer/stealth-crypto/src/stealth_address.rs` — full stealth address implementation
- **Learnings for future iterations:**
  - k256 `PublicKey::as_affine()` returns a reference; use `*pubkey.as_affine()` to copy into `ProjectivePoint::from()`
  - `Scalar::reduce()` requires `k256::elliptic_curve::ops::Reduce` trait import and `k256::U256::from_be_slice()`
  - ECDH shared secret is computed as `keccak256(compressed_point_encoding)` — both sides get identical results because scalar multiplication is commutative: r*V = r*(v*G) = v*(r*G) = v*R
  - The `user_generate_stealth` function returns the shared_secret alongside the StealthAddress — this is needed for instruction encryption in US-003
  - 8 tests cover: key gen, ECDH agreement, stealth derivation, private key recovery, deterministic output, different ephemeral keys, wrong relayer rejection, address format
---

## 2026-02-13 - US-003
- What was implemented: Instruction encryption module with AES-256-GCM and HKDF key derivation
- Files changed:
  - `relayer/stealth-crypto/Cargo.toml` — added aes-gcm, hkdf, sha2 dependencies
  - `relayer/stealth-crypto/src/lib.rs` — added instruction module export
  - `relayer/stealth-crypto/src/instruction.rs` — Instruction struct, serialization, encryption, decryption, commitment
  - `relayer/Cargo.lock` — updated lockfile
- **Learnings for future iterations:**
  - aes-gcm crate appends the 16-byte GCM authentication tag to the ciphertext internally — no need to handle it separately
  - `Nonce::from_slice` triggers a deprecation warning from generic-array 0.x in aes-gcm 0.10 — this is upstream and can be ignored
  - HKDF with `info=b"stealth-defi-encryption"` and no salt is used for key derivation — keep consistent across Rust and any future Solidity/JS implementations
  - Instruction serialization is 141 bytes: 1 (action_type) + 20*3 (addresses) + 32*2 (U256s) + 8*2 (u64s)
  - The `commitment()` function reuses the existing `keccak256` from `stealth_address` module — imported via `crate::keccak256`
  - 8 tests cover: serialization round-trip, encryption round-trip, wrong key, tampered ciphertext (GCM auth), deterministic commitment, different nonces, HKDF determinism, wrong-length deserialization
---

## 2026-02-13 - US-004
- What was implemented: StealthEscrow.sol — commitment + bond contract with challenge period and fund recovery
- Files changed:
  - `contracts/src/StealthEscrow.sol` — main contract: postCommitment, releaseBond, challenge, recoverFunds
  - `contracts/test/StealthEscrow.t.sol` — 12 Foundry tests covering all acceptance criteria
  - `contracts/foundry.toml` — pinned solc_version to 0.8.24 (0.8.21 has Natspec ICE bug)
- **Learnings for future iterations:**
  - Solc 0.8.21 (Foundry nightly default) triggers an Internal Compiler Error on Natspec processing — fix by pinning `solc_version = "0.8.24"` in foundry.toml
  - `vm.warp(block.timestamp + N)` in Foundry tests manipulates block timestamp for time-based tests
  - `vm.deal(addr, amount)` pre-funds test addresses with ETH
  - `vm.expectEmit(true, true, false, true)` followed by `emit Event(...)` pattern verifies event emission
  - StealthEscrow stores commitments with auto-incrementing ID via `nextCommitmentId++`
  - The `verifyAndRelease` function (for US-008 ZKP integration) is not yet added — will extend StealthEscrow when Groth16 verifier is ready
  - 12 tests cover: post commitment, event emission, low bond rejection, release after deadline, early release rejection, non-relayer release rejection, double release rejection, challenge, late challenge rejection, fund recovery after timeout, early recovery rejection, multiple commitments
---

## 2026-02-13 - US-005
- What was implemented: MockERC20 (minimal ERC20 with public mint) and MockAMM (constant-product x*y=k AMM) contracts, deploy script, and comprehensive Foundry tests
- Files changed:
  - `contracts/src/MockERC20.sol` — minimal ERC20 with public mint, approve, transfer, transferFrom
  - `contracts/src/MockAMM.sol` — constant-product AMM with addLiquidity, swap, getAmountOut; simple LP tracking (not ERC20)
  - `contracts/script/DeployMockAMM.s.sol` — deploys TokenA, TokenB, MockAMM; mints tokens and adds 100k initial liquidity
  - `contracts/test/MockAMM.t.sol` — 11 tests covering all acceptance criteria
- **Learnings for future iterations:**
  - MockAMM reserves are updated from actual balances after swap (`tokenA.balanceOf(address(this))`) — ensures reserves match reality even if tokens are sent directly
  - Constant product formula: `amountOut = (amountIn * reserveOut) / (reserveIn + amountIn)` — no fees means k always increases slightly due to integer rounding
  - MockERC20 uses `type(uint256).max` allowance check to support infinite approvals
  - First LP deposit uses `amountA` as the LP baseline; subsequent deposits are proportional to existing reserves
  - 11 tests cover: add liquidity, second LP provider, swap A→B, swap B→A, slippage revert, zero amount revert, getAmountOut correctness, invalid token revert, swap event emission, zero liquidity add revert, constant product invariant
---

## 2026-02-13 - US-006
- What was implemented: Circom circuit for instruction commitment proof using Poseidon hash
- Files changed:
  - `circuits/instruction_commitment.circom` — ZK circuit: 8 private inputs (instruction fields) + 1 public input (commitment_hash), Poseidon(8) hash constraint
  - `scripts/compile.sh` — compiles circuit to R1CS + WASM, logs constraint count via snarkjs
  - `scripts/test_witness.sh` — generates witness with known test inputs and verifies against R1CS
  - `scripts/compute_poseidon.js` — JS helper using circomlibjs to compute Poseidon hash and generate input.json
  - `package.json` — added circomlibjs as devDependency
- **Learnings for future iterations:**
  - Poseidon(8) on BN128 produces 1171 constraints (78 template instances, 405 non-linear + 766 linear constraints)
  - `Poseidon(nInputs)` in circomlib uses `PoseidonEx(nInputs, 1)` with `initialState=0` — supports up to 16 inputs
  - The `N_ROUNDS_P` array in poseidon.circom has 16 entries for t=2..17, where t = nInputs + 1
  - circomlibjs `buildPoseidon()` returns an async function; hash output needs `F.toObject(hash).toString()` to get the decimal string for input.json
  - Circuit public inputs use syntax: `component main {public [commitment_hash]} = InstructionCommitment()`
  - `npx snarkjs wtns check` verifies a witness satisfies all R1CS constraints — essential for testing
  - The witness WASM generator is in `circuits/build/instruction_commitment_js/generate_witness.js`
---
