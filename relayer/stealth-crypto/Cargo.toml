[package]
name = "stealth-crypto"
version = "0.1.0"
edition = "2024"

[dependencies]
k256 = { version = "0.13", features = ["ecdh", "arithmetic"] }
tiny-keccak = { version = "2.0", features = ["keccak"] }
rand = "0.8"
hex = "0.4"
